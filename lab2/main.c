//lab 2.3

#include <stdio.h>
#include "stack.h"

int main(int argc, const char** argv) {

	return 0;
	// Stack stack = stack_new(sizeof(int));


	// int a = 10;
	// stack_push(stack, &a);
	// stack_print_int(stack);

	// a = 30;
	// stack_push(stack, &a);
	// stack_print_int(stack);

	// a = 20;
	// stack_peek(stack, &a);
	// stack_print_int(stack);

	// stack_push(stack, &a);
	// stack_print_int(stack);

	// stack_pop(stack, &a);
	// stack_print_int(stack);

	// stack_delete(stack);
}